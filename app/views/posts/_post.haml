%div{:id => post.id}
  %h4
    = link_to image_tag(File.join('avatars', "#{post.nickname.parameterize.to_s}.png")), user_path(post.nickname.parameterize.to_s)
  %div.bubble
    =raw format_text(post.content)
  %hr
