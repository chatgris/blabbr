!!! Strict
%html
  %head
    %title
      Login to blabbr
    = stylesheet_link_tag 'sessions'
  %body
    %h1.center
      blabbr
    - flash.each do |name, msg|
      .center
        = content_tag :div, msg, :id => "flash_#{name}"
    #content
      .center
        =yield
